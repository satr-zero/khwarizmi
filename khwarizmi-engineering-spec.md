# مواصفة هندسية شاملة — مشروع "خوارزمي" (Khwarizmi)
### AI Agent مكتبي مستقل لنظام Windows — منافس لأطر مثل Hermes Agent و OpenClaw و Pi

---

## 0. الدور المطلوب منك (أيها المنفذ)

أنت مهندس برمجيات مكلّف ببناء **خوارزمي**: تطبيق Flutter لسطح مكتب Windows يعمل كـ AI Agent شخصي حقيقي — لا مجرد واجهة دردشة فوق API. الفرق الجوهري: خوارزمي يملك **ذاكرة دائمة فعلية**، **أدوات حقيقية** (جدولة، إشعارات نظام، بحث، تصفح)، ويستقل عن أي مزوّد ذكاء اصطناعي واحد.

اقرأ هذه الوثيقة كاملة قبل البدء. لا تبدأ التنفيذ الفعلي قبل أن تعرض عليّ خطة مرحلية (Phases) مبنية على القسم 8 أدناه وتنتظر موافقتي.

**ملاحظة مهمة:** شخصية الوكيل وأسلوب حديثه وقواعده السلوكية (اسمه "خوارزمي"، طريقة تعامله، حدوده الأخلاقية) موجودة في ملف منفصل اسمه `AGENT.md` سيُقدَّم لاحقًا بشكل مستقل. هذه الوثيقة تغطي **الهندسة والبنية التقنية فقط**. لا تخلط بين الاثنين: `AGENT.md` يُحمَّل كـ system prompt وقت التشغيل، وليس جزءًا من الكود الثابت.

---

## 1. الفلسفة والموقع التنافسي

خوارزمي لا ينافس عبر كثرة الميزات، بل عبر مبدأين:

1. **لا ينسى أبدًا** — كل معلومة صغيرة يذكرها المستخدم تُسجَّل وتُسترجَع بذكاء وقت الحاجة (ذاكرة دلالية حقيقية، لا سياق محادثة عابر).
2. **أداة فعلية لا ديكور** — كل ما "يعد" به الوكيل ينفَّذه فعليًا على مستوى نظام التشغيل: تنبيه حقيقي يظهر، رسالة تُرسَل فعلًا، بحث حقيقي على الويب، تصفح حقيقي لصفحة.

مرجعان لدراسة نمط التصميم (بدون نسخ الكود، فقط الفلسفة المعمارية):
- **Pi (by Mario Zechner / Earendil Works)**: نواة صغيرة جدًا (4 أدوات أساسية فقط)، وسّع عبر إضافات، شفافية كاملة بالـ context، لا تعقيد مفروض على المستخدم.
- **Hermes Agent (Nous Research)**: ذاكرة ثلاثية الطبقات (episodic / semantic / procedural)، يعمل بشكل مستمر (persistent)، بيانات محلية بالكامل بدون سحابة.

القاعدة الذهبية: **البساطة بالواجهة، القوة بالخلفية.** لا تُثقل تجربة المستخدم أبدًا بتعقيد الأدوات الداخلية.

---

## 2. نظرة عامة على المعمارية (4 طبقات + طبقة أدوات)

```
┌─────────────────────────────────────────────────┐
│  1. واجهة المستخدم (Flutter Windows Desktop)      │
├─────────────────────────────────────────────────┤
│  2. طبقة تجريد مزوّدي الذكاء الاصطناعي              │
│     (Claude / OpenAI / Gemini / أي مزوّد آخر)      │
├─────────────────────────────────────────────────┤
│  3. محرّك الأدوات (Tool-Calling Engine)            │
│     ├─ الذاكرة طويلة الأمد (SQLite + Vector)      │
│     ├─ الجدولة والتنبيهات (Windows Notifications) │
│     ├─ البحث على الويب (Web Search)                │
│     └─ المتصفح المستقل للوكيل (Agent Browser)      │
├─────────────────────────────────────────────────┤
│  4. الصوت (STT / TTS) — مرحلة لاحقة                │
└─────────────────────────────────────────────────┘
```

**مبدأ تصميمي أساسي:** الطبقات 2، 3، 4 يجب أن تكون **مستقلة عن واجهة المستخدم تمامًا** (Clean Architecture / Ports & Adapters). أي طبقة تجريد يجب أن يكون بالإمكان اختبارها واستدعاؤها دون تشغيل الواجهة الرسومية إطلاقًا.

---

## 3. الطبقة 1 — واجهة المستخدم (Flutter Windows)

### المتطلبات
- تطبيق Flutter Desktop لنظام Windows حصرًا في المرحلة الحالية (لا حاجة لدعم أندرويد/iOS الآن، لكن ابنِ الكود بحيث لا يستحيل توسيعه لاحقًا — تجنّب أي كود مربوط حصريًا بـ Win32 خارج طبقة الأدوات).
- شاشة دردشة رئيسية (chat interface) شبيهة بواجهات Claude/ChatGPT: فقاعات رسائل، بث تدريجي للرد (streaming)، عرض استدعاءات الأدوات بشكل مرئي واضح (مثلاً: "🔍 يبحث في الويب..."، "⏰ يجدول تذكيرًا...").
- شاشة إعدادات: اختيار مزوّد الذكاء الاصطناعي، إدخال/تبديل مفاتيح API، إدارة الذاكرة (عرض/حذف عناصر مسجّلة)، إدارة الأذونات (إشعارات، متصفح).
- شاشة عرض التذكيرات/الجدولة النشطة.
- دعم تعدد النوافذ أو Tabs إذا لزم لعرض المتصفح المستقل للوكيل (انظر القسم 7).

### تقنيات مقترحة
- `flutter` (قناة stable، دعم Windows desktop مفعّل).
- إدارة الحالة: `riverpod` أو `bloc` (اختر واحدة والتزم بها لكامل المشروع).
- تخزين الإعدادات غير الحساسة: `shared_preferences`. المفاتيح الحساسة (API keys) يجب تخزينها عبر آلية تشفير محلية (`flutter_secure_storage` أو ما يعادلها على Windows عبر DPAPI) — **لا تُخزَّن كنص صريح أبدًا**.

---

## 4. الطبقة 2 — تجريد مزوّدي الذكاء الاصطناعي (Provider-Agnostic Layer)

### المتطلب الأساسي
المستخدم يجب أن يقدر يبدّل مزوّد الذكاء الاصطناعي (Claude، OpenAI، Gemini، أو أي مزوّد متوافق مع OpenAI-style API) **من الإعدادات مباشرة**، بدون أي تعديل كود، وبدون أن ينكسر أي شيء في نظام الأدوات أو الذاكرة.

### التصميم المطلوب
ابنِ واجهة (interface/abstract class) موحّدة، مثلًا:

```dart
abstract class AiProvider {
  String get id;
  Stream<AgentEvent> sendMessage({
    required List<ChatMessage> history,
    required List<ToolDefinition> availableTools,
    required String systemPrompt,
  });
}
```

ثم نفّذ implementation منفصل لكل مزوّد (`ClaudeProvider`, `OpenAiProvider`, `GeminiProvider`...). كل implementation مسؤول عن:
- تحويل صيغة الأدوات (tool schemas) إلى الصيغة الخاصة بذلك المزوّد (كل مزوّد له صيغة tool-calling مختلفة قليلاً).
- تحويل الرد (streaming أو non-streaming) إلى صيغة موحّدة داخلية (`AgentEvent`: نص، استدعاء أداة، نتيجة أداة، نهاية الرد).
- التعامل مع أخطاء المزوّد (rate limits، انقطاع الاتصال، مفتاح غير صالح) بشكل موحّد يُعرَض للمستخدم بوضوح.

### معيار قبول
إضافة مزوّد جديد لاحقًا يجب أن تتطلب فقط: كتابة implementation واحد جديد + سطر تسجيل (registration) — بدون لمس أي كود في طبقة الأدوات أو الواجهة.

---

## 5. الطبقة 3أ — الذاكرة طويلة الأمد (الميزة الأهم)

### المتطلب الوظيفي
كل معلومة يذكرها المستخدم — مهما كانت صغيرة — تُسجَّل تلقائيًا، وتُسترجَع بذكاء وقت الحاجة عبر بحث دلالي (semantic search)، لا بحث نصي حرفي فقط.

### البنية التقنية
- **قاعدة البيانات:** SQLite محليًا على جهاز المستخدم (لا سحابة، خصوصية كاملة). استخدم حزمة `sqlite3` أو `drift` لإدارة المخطط (schema) والاستعلامات بأمان.
- **التخزين الدلالي (Vector Search):** خزّن embedding vector لكل عنصر ذاكرة، وابحث بالتشابه (cosine similarity). خياران معماريان — اعرضهما عليّ ودعنا نقرر معًا وقت الوصول لهذه المرحلة الفعلية، لا تفترض مسبقًا:
  1. **مكتبة SQLite موسّعة بدعم المتجهات** (مثل `sqlite-vec` عبر FFI) — تبقي كل شيء بملف قاعدة بيانات واحد، أبسط للنشر.
  2. **فهرسة متجهات منفصلة في الذاكرة** (in-process vector index) تُحمَّل من SQLite عند الإقلاع — أسرع بالبحث لكن يحتاج إدارة تزامن أدق.
- **توليد الـ Embeddings محليًا (بدون إنترنت):** هذه أصعب نقطة تقنية بالمشروع. الخياران المطروحان:
  1. **ONNX Runtime داخل Flutter مباشرة** (عبر binding Dart لـ ONNX Runtime) لتشغيل نموذج embedding صغير (مثل `all-MiniLM-L6-v2` المحوَّل لصيغة ONNX).
  2. **Sidecar process منفصل وخفيف** (مثلاً عملية صغيرة مبنية بلغة أخرى مناسبة لتشغيل نماذج ML، تتواصل مع تطبيق Flutter عبر IPC/socket محلي).

  **لا تختر تلقائيًا — قدّم لي مقارنة عملية موجزة (سهولة تكامل، حجم النشر النهائي، الأداء) عند الوصول الفعلي لهذه المرحلة، وسنقرر معًا.**

### مخطط بيانات مقترح لعنصر الذاكرة (Memory Entry)
```
id, content (النص الخام), embedding (vector),
category (تصنيف اختياري), source_conversation_id,
created_at, last_accessed_at, importance_score (اختياري)
```

### آلية الاسترجاع وقت المحادثة
قبل إرسال أي رسالة للمزوّد، نفّذ بحث دلالي في قاعدة الذاكرة عن العناصر الأكثر صلة بالسياق الحالي، وأدرجها ضمن الطلب (كسياق إضافي أو كنتيجة استدعاء أداة `search_memory`). لا تُحمَّل الذاكرة كاملة في كل طلب — هذا غير عملي وغير فعّال.

---

## 6. الطبقة 3ب — الجدولة والتنبيهات الحقيقية (Windows Notifications)

### المتطلب
عندما يطلب المستخدم "ذكّرني بكذا الساعة كذا"، يجب أن يظهر **إشعار Windows حقيقي** على مستوى نظام التشغيل (Windows Notification Center)، وليس مجرد رسالة داخل التطبيق.

### تقنيات مؤكدة ومتاحة حاليًا
- **`local_notifier`**: حزمة Flutter تدعم إشعارات سطح المكتب على Windows/macOS/Linux، تدعم `onShow`/`onClose`/`onClick` callbacks.
- **`flutter_local_notifications`**: بديل شائع يدعم Windows أيضًا ضمن منصّاته المدعومة.

اختر واحدة (يُفضَّل `local_notifier` لأنها مبنية خصيصًا لتجربة سطح المكتب) وابنِ فوقها:
- **محرّك جدولة داخلي** (Scheduler Service) يعمل في الخلفية طالما التطبيق يعمل، يتحقق دوريًا من المهام المجدولة المخزَّنة في SQLite ويُطلق التنبيه بالوقت الصحيح.
- **دعم التكرار** (يومي، أسبوعي، تاريخ محدد) — خزّن قاعدة التكرار (recurrence rule) وليس فقط الوقت التالي.
- معالجة حالة إغلاق التطبيق: هل التطبيق يعمل بالخلفية دائمًا (system tray) أم يعتمد على Windows Task Scheduler لإطلاقه عند وقت التنبيه؟ **اقترح الحل الأنسب وناقشه معي قبل التنفيذ** — الخيار الشائع هو تصغير التطبيق لـ System Tray بدل إغلاقه فعليًا (استخدم حزمة مثل `tray_manager` أو ما يعادلها لدعم System Tray على Windows).

---

## 7. الطبقة 3ج — البحث والتصفح (الويب)

هذا القسم له جزآن منفصلان وظيفيًا — لا تدمجهما بنفس الأداة:

### 7.1 أداة بحث ويب بسيطة (Web Search Tool)
أداة يستدعيها الوكيل تلقائيًا أثناء المحادثة العادية عندما يحتاج معلومة حالية (أخبار، أسعار، معلومة لا يعرفها). هذه أداة **خفيفة وسريعة**: استعلام نصي → نتائج (عنوان، رابط، مقتطف) تُرجَع للنموذج مباشرة كنص، بدون فتح أي واجهة مرئية. يمكن تنفيذها عبر أي API بحث خارجي (Bing Search API، أو أي مزوّد بحث يدعمه المزوّد نفسه إن وُجد).

### 7.2 المتصفح المستقل للوكيل (Agent's Own Browser) — ميزة أساسية مطلوبة
هذه ميزة أعمق: **متصفح فعلي مدمج بالتطبيق، بحساب/جلسة خاصة بالوكيل نفسه**، منفصلة عن متصفح المستخدم الشخصي. الهدف: الوكيل (أو المستخدم بإشرافه) يقدر:
- يتصفح مواقع فعلية ويتفاعل معها (نقر، تعبئة نماذج، تسجيل دخول).
- يبحث عن عروض/أسعار على مواقع تجارية.
- يجلب أخبار أو معلومات محدّثة من صفحات فعلية (لا بحث نصي فقط، بل قراءة صفحة كاملة).
- يحتفظ بجلسة/كوكيز منفصلة خاصة به (حساب مستقل يمكن للمستخدم ربطه لاحقًا — مثلاً حساب بريد أو حساب تسوّق مخصّص للوكيل).

### التقنية المؤكدة
- **`webview_flutter_windows`**: حزمة مبنية على Microsoft Edge WebView2، تعرض المتصفح كـ Texture ضمن شجرة الواجهة (لا نافذة منفصلة تطفو فوق التطبيق)، وتدعم التحكم الكامل بالتنقل، تنفيذ JavaScript، وإدارة الكوكيز.
- **بنية الجلسة المنفصلة:** كل WebView2 instance يمكن ربطه بـ **User Data Folder** منفصل تمامًا عن أي متصفح آخر على الجهاز — هذا يحقق مطلب "حساب/جلسة خاصة بالوكيل" بشكل طبيعي من دعم WebView2 نفسه، دون الحاجة لأي حل خارجي معقّد.
- **التحكم الآلي (Agentic control):** الوكيل يتفاعل مع الصفحة عبر تنفيذ JavaScript داخل الـ WebView (`executeScript`) لقراءة محتوى الصفحة، النقر على عناصر، تعبئة حقول. ابنِ طبقة "قراءة محتوى الصفحة المبسّط" (extract visible text/links) تُعاد للنموذج كسياق نصي مفهوم، بدل إغراقه بـ HTML خام.

### اعتبار أمني مهم يجب مراعاته بالتصميم
- أي عملية "شراء" أو إجراء له أثر مالي/التزامي حقيقي (إتمام طلب، إدخال بيانات دفع) **يجب أن يتطلب تأكيدًا صريحًا من المستخدم قبل التنفيذ** — لا تصميم يسمح للوكيل بإتمام معاملات مالية بشكل مستقل تلقائيًا دون موافقة لحظية من المستخدم على تلك الخطوة تحديدًا.
- بيانات تسجيل الدخول المحفوظة بجلسة المتصفح المستقل يجب أن تُخزَّن مشفّرة على القرص، بنفس مستوى الحماية المطلوب لمفاتيح API.

---

## 8. الصوت (STT / TTS) — مرحلة لاحقة، غير مطلوبة بالإصدار الأول

وثّق فقط أن البنية (خصوصًا طبقة الأدوات وحلقة المحادثة) يجب ألا تفترض أن المدخل/المخرج نص دائمًا — اجعل نقطة الدخول للمحادثة قابلة للتوسع لاحقًا لقبول صوت محوَّل لنص (عبر أي محرك STT محلي أو سحابي يُقرَّر لاحقًا) وإخراج نص محوَّل لصوت (TTS). لا تنفّذ هذا الآن، فقط لا تصمّم بشكل يمنعه لاحقًا.

---

## 9. متطلبات غير وظيفية

| المتطلب | التفصيل |
|---|---|
| **الخصوصية** | الذاكرة والبيانات الشخصية تبقى محليًا على جهاز المستخدم دائمًا. لا رفع بيانات ذاكرة لأي سحابة خارجية (باستثناء ما يُرسَل حصرًا كسياق لمزوّد الذكاء الاصطناعي المختار وقت المحادثة، وهذا واضح وشفاف للمستخدم). |
| **الأمان** | مفاتيح API وبيانات جلسة المتصفح مشفّرة على القرص. لا تسجيل (logging) لمحتوى حساس بملفات نصية عادية. |
| **البساطة** | أي ميزة جديدة يجب أن تُقيَّم بمعيار Pi: هل تزيد التعقيد على المستخدم النهائي؟ إن كانت الإجابة نعم، ابحث عن طريقة لإخفاء التعقيد خلف الكواليس. |
| **قابلية التوسع للمزوّدين** | إضافة مزوّد ذكاء اصطناعي جديد = implementation واحد جديد فقط (انظر القسم 4). |
| **الاستقلالية عن الاتصال** | الذاكرة والجدولة والإشعارات يجب أن تعمل بدون إنترنت. فقط طلبات المحادثة الفعلية للمزوّد والبحث/التصفح على الويب تحتاج اتصالًا. |

---

## 10. خارطة الطريق المرحلية المقترحة (Phases)

**لا تنفّذ كل هذا دفعة واحدة.** اعرض علي هذه الخطة (أو نسخة معدَّلة منها إن رأيت تسلسلًا أفضل) وابدأ فقط بعد الموافقة الصريحة:

- **المرحلة 0 — الأساس:** هيكلة المشروع (Clean Architecture)، طبقة تجريد المزوّدين مع مزوّد واحد فقط في البداية (Claude)، واجهة دردشة بسيطة تعمل بدون أي أدوات بعد.
- **المرحلة 1 — الذاكرة الأساسية:** قاعدة SQLite، تخزين/استرجاع نصي بسيط (بدون vector بعد) لإثبات أن الحلقة الكاملة (حفظ → استرجاع → حقن بالسياق) تعمل.
- **المرحلة 2 — الذاكرة الدلالية:** إضافة الـ embeddings المحلية والبحث الدلالي الفعلي فوق ما بُني بالمرحلة 1.
- **المرحلة 3 — الجدولة والتنبيهات:** تكامل `local_notifier` + محرّك الجدولة + System Tray.
- **المرحلة 4 — تعدد المزوّدين:** إضافة OpenAI وGemini كمزوّدين بديلين، واختبار تبديلهم من الإعدادات.
- **المرحلة 5 — البحث على الويب:** أداة بحث بسيطة تُستدعى تلقائيًا أثناء المحادثة.
- **المرحلة 6 — المتصفح المستقل:** تكامل `webview_flutter_windows` بجلسة منفصلة + طبقة قراءة/تفاعل مع الصفحات.
- **المرحلة 7 — الصوت:** STT/TTS (تُفصَّل لاحقًا عند الوصول لهذه المرحلة).

---

## 11. ملاحظات معمارية ختامية

- **افصل دائمًا** بين: (أ) هوية الوكيل الثابتة (`AGENT.md` — يُحمَّل كنص system prompt)، و(ب) ذاكرة المستخدم المتراكمة (SQLite — بيانات، لا تعليمات سلوك). لا تكتب حقائق عن المستخدم داخل ملف الشخصية أبدًا.
- **صمّم من اليوم الأول** بحيث يكون خوارزمي قابلًا للتوزيع لاحقًا كمنتج مستقل (مثل Hermes/Pi) — أي مستخدم جديد يشغّله يبدأ بذاكرة فارغة وهوية "خوارزمي" نفسها.
- عند كل قرار تقني غامض أو له أكثر من حل معقول (خصوصًا في قسم الذاكرة الدلالية)، **اعرض الخيارات ومفاضلاتها العملية بإيجاز قبل التنفيذ، ولا تفترض قرارًا نهائيًا من تلقاء نفسك.**
